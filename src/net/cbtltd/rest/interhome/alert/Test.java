package net.cbtltd.rest.interhome.alert;

/**
 * Please modify this class to meet your needs
 * This class is not complete
 */

import javax.xml.bind.JAXBContext;
import javax.xml.bind.Unmarshaller;

import net.cbtltd.server.RazorServer;
import net.cbtltd.server.ServiceException;
import net.cbtltd.server.api.AlertMapper;
import net.cbtltd.server.api.ProductMapper;
import net.cbtltd.server.project.PartyIds;
import net.cbtltd.shared.Error;
import net.cbtltd.shared.NameId;
import net.cbtltd.shared.Product;
import net.cbtltd.shared.api.HasUrls;

import org.apache.ibatis.session.SqlSession;

/**
 * This class was generated by Apache CXF 2.4.0
 * 2012-08-09T13:20:11.648+02:00
 * Generated source version: 2.4.0
 * 
 */
public final class Test {

	private Test() {}

	public static void main(String args[]) throws Exception {
		SqlSession sqlSession = RazorServer.openSession();
		try {
			JAXBContext jc = JAXBContext.newInstance("net.cbtltd.rest.interhome.alert");
			Unmarshaller um = jc.createUnmarshaller();
			
			Alerts alerts = (Alerts) um.unmarshal(new java.io.FileInputStream( "C:/alert_en.xml" ));

			int i = 0;
			for (Alert alert : alerts.getAlert()) {
				System.out.println(i++ + " " + alert);
				net.cbtltd.shared.Alert action = new net.cbtltd.shared.Alert();
				Product product = sqlSession.getMapper(ProductMapper.class).altread(new NameId(PartyIds.PARTY_INTERHOME_ID, alert.getCode()));
				if (product == null) {throw new ServiceException(Error.product_id, alert.getCode());}
				action.setEntitytype(NameId.Type.Product.name());
				action.setEntityid(product.getId());
				action.setName(alert.getText());
				action.setFromdate(alert.getStartdate().toGregorianCalendar().getTime());
				action.setTodate(alert.getEnddate().toGregorianCalendar().getTime());
				sqlSession.getMapper(AlertMapper.class).create(action);
				sqlSession.commit();
			}
		} catch (Throwable x) {x.printStackTrace();}

		System.out.println("Finished...");
		System.exit(0);
	}
}
